# device-notebook-pro

مفكرة ذكية لإدارة الأجهزة بكل سهولة

تطبيق أندرويد احترافي يدعم اللغة العربية RTL لتسجيل أجهزة الكمبيوتر والتقنيات الأخرى يدويًا. يتيح إضافة وتعديل وإدارة الأجهزة مع إمكانية البحث والتصفية حسب النوع والموقع، بالإضافة إلى خاصية الأفضليات وتخصيص الأيقونات. يعتمد على معايير Material Design 3 وJetpack Compose لضمان تجربة مستخدم سلسة ومتكاملة.

## 🚀 التشغيل المحلي / Local Development

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # production bundle → dist/
npm run preview  # preview the production build
```

## 🔌 Backend

هذا التطبيق يحتاج إلى **API خارجي**. انسخ `.env.example` إلى `.env` وحدّد `VITE_API_BASE_URL`.

The frontend expects an external API. Copy `.env.example` to `.env` and set `VITE_API_BASE_URL`.


## 📱 تحويل المشروع إلى APK / Build Android APK

### الطريقة الأولى: محليًا (يحتاج JDK 17 + Android SDK)
```bash
npm install
npm run android:init     # أول مرة فقط — يضيف مجلد android/
npm run android:sync     # build + cap sync
npm run android:apk      # ينتج app-debug.apk
```
الناتج: `android/app/build/outputs/apk/debug/app-debug.apk`

### الطريقة الثانية: عبر GitHub Actions (بدون أي إعداد محلي)
ارفع المشروع إلى GitHub، وسيقوم `.github/workflows/android-apk.yml` ببناء الـ APK تلقائيًا وإتاحته كـ artifact.

### الطريقة الثالثة: PWABuilder
ارفع البناء إلى أي استضافة، ثم استخدم <https://www.pwabuilder.com/> لتوليد APK سريعًا.


---
_Generated by Alborihi AI Studio · App Generator_
